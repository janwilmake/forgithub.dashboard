{
  "scripts": {
    "backup": "npx wrangler d1 export sponsorflare --remote --output backup.sql && npx wrangler d1 execute --local sponsorflare --file backup.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250124.3"
  },
  "dependencies": {
    "sponsorflare": "^0.0.8"
  }
}
